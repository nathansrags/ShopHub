server:
  port: 9092
  servlet:
    context-path: /authenticate
spring:
  application:
    name: CUSTOMER-API
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/ShoppingServer/eureka/}
      register-with-eureka: true
      fetch-registry: true
  instance:
    prefer-ip-address: true
management:
  endpoints:
    web:
      exposure:
        include: "health, info"

