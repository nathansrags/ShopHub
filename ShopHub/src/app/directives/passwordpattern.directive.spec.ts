import { PasswordpatternDirective } from './passwordpattern.directive';

describe('PasswordpatternDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordpatternDirective();
    expect(directive).toBeTruthy();
  });
});
